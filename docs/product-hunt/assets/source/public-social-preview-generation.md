# Public website social-preview generation record

Output: `../social-preview-public-1280x640.png`

The built-in OpenAI image generation tool edited the earlier draft preview with
the canonical SideRefresh icon as a second reference. The selected result was
resized without cropping to 1280×640 and visually inspected for copy, logo,
layout, and prohibited decoration. It contains no real user or device data.

## Selected prompt

> Create a 1280×640 social preview for SideRefresh. Replace the dark 3D draft
> with a restrained light macOS utility composition on flat `#F4F5F7`. Keep the
> canonical icon visually faithful. Use ink `#17191D`, gray `#606771`, rule
> `#D9DDE3`, blue `#1267D6`, and green `#16845B`. Set the exact text
> “SideRefresh”, “Keep your personal iOS app running.”, “Open source Mac app ·
> One app · One iPhone”, “Verified install”, “Refresh before expiry”, and
> “Signing expires”. Place the final three labels on a thin evidence rail.
> Avoid gradients, glow, glass, 3D objects, orbit graphics, decorative blobs,
> stock illustration, fake UI, badges, shadows, watermark, Product Hunt
> branding, and draft labels.

The website itself remains static HTML and CSS. This raster asset is used only
for link previews; it does not determine page layout or product behavior.
