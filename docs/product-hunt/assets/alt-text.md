# Launch asset alt text

Use these descriptions where the publishing surface supports authored alt text.
Update them if the final release-candidate images change materially.

1. **Core outcome:** SideRefresh app icon beside an abstract glowing renewal
   loop, with the message that an agent builds the iOS app and SideRefresh
   keeps it running.
2. **Expiration cycle:** Four-step diagram—Build, Install, Use, Refresh—showing
   that a free Personal Team development app must be refreshed before signing
   expires.
3. **Simple workspace:** Dark-mode SideRefresh DEBUG fixture mirroring the
   checked-in SideRefresh Sample app, a synthetic Demo iPhone, automatic refresh
   ready, signing dates, the Xcode connection method, and a verified result.
4. **Supported flow:** Four stages from a user-owned Xcode project through
   SideRefresh and Xcode signing/CoreDevice to a paired iPhone.
5. **Local boundary:** Three cards explain local source, no Apple Account
   password collection, and explicit project and device access.
6. **Open-source trust:** SideRefresh icon with MIT-licensed Swift source,
   the public-repository launch gate, English and Korean UI, and the signed and
   notarized public-release gate.
7. **Social preview:** SideRefresh icon and abstract renewal loop beside the
   tagline “Keep agent-built iOS apps alive on your iPhone.”
8. **Video thumbnail:** SideRefresh's Simple workspace sample fixture beside the
   title “Keep your personal iOS app running” and a play indicator.
9. **Public-site social preview:** SideRefresh icon and name above “Keep your
   personal iOS app running,” followed by a rail for verified installation,
   refresh before expiry, and signing expiration.
