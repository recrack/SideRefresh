# v0.2.0-beta.1 source-prerelease acceptance

- Date: 2026-08-06
- Channel: source-only GitHub prerelease
- Planned tag: `v0.2.0-beta.1`
- App version/build: `0.2.0 (2)`
- Reviewer and publisher: `@recrack`
- Human authorization: `@recrack` explicitly approved pushing and publishing
  this source-only prerelease on 2026-08-06.

## Candidate preparation

The candidate was prepared from a clean worktree at the public `master` tip.
No Developer ID binary, ad-hoc app bundle, provisioning profile, signing
credential, device identifier, or private project data is attached to this
prerelease.

The following checks passed before the release change:

- public-source validator tests and public-source validation;
- 10 Python tests;
- 335 Swift tests;
- iOS sample validation without signing or device installation;
- Headless CLI, LaunchAgent plan, and MCP protocol validation;
- Product Hunt asset and public-site validation;
- public-site validator and homepage design tests; and
- GitHub Pages publication tests.

The same checks must pass for the release commit in pull-request CI before the
tag is created.

## Gate status

| Gate | Status before publication | Evidence |
| --- | --- | --- |
| `SRC-01` public source and repository controls | pass | Public-source validators, MIT and community files, public repository review |
| `SRC-02` source-only prerelease preparation | pass | Versioned notes, source-build commands, no binary asset |
| `SRC-03` requirements, privacy, scope, and support | pass | Release notes explicitly record that no real iPhone, iOS version, or connection route was verified for the exact source candidate. Requirements, privacy, limitations, removal, support, and security paths are documented without inferring compatibility. |
| `SRC-04` immutable public tag and source archives | not-run | Record the public tag, generated archives, reviewed commit, prerelease status, and immutability on [issue #6](https://github.com/recrack/SideRefresh/issues/6) immediately after publication. |

Automated Simple-workspace, migration, renewal, failure, and semantic UI tests
pass as part of the 335-test Swift suite. Every human-fixture and real-device
row remains `not-run` for this exact candidate; none is inferred from automated
coverage or turned into a compatibility claim. Stable `v0.2.0` remains blocked
on Developer ID signing, notarization, stapling, exact-archive Gatekeeper
checks, clean-account testing, and final real-device acceptance.
