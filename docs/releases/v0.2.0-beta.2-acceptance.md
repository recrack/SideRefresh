# v0.2.0-beta.2 source-prerelease acceptance

- Date: 2026-08-08
- Channel: source-only GitHub prerelease
- Planned tag: `v0.2.0-beta.2`
- App version/build: `0.2.0 (3)`
- MCP server version: `0.2.0-beta.2`
- Reviewer and publisher: `@recrack`
- Human authorization: `@recrack` explicitly approved adopting Apache License
  2.0 and publishing this prerelease on 2026-08-08.

## Candidate preparation

The candidate was prepared from the public repository's `master` tip. It
changes licensing and distribution metadata without changing renewal behavior
or adding a downloadable Mac application.

The following checks passed before publication:

- public-source validator tests and public-source validation;
- 10 Python tests;
- 335 Swift tests with zero failures;
- iOS sample validation without signing or device installation;
- Headless CLI, LaunchAgent plan, and MCP protocol validation;
- Product Hunt asset and public-site validation;
- public-site validator and homepage design tests;
- GitHub Pages publication tests;
- app and Headless release builds with valid ad-hoc signatures; and
- packaged `LICENSE`, `NOTICE`, and `BRAND_POLICY.md` files matching the
  repository copies.

The same repository checks must pass in pull-request CI before the tag is
created.

## License boundary

- Source code and prose documentation use Apache License 2.0 beginning with
  this release.
- The SideRefresh name and visual identity follow `BRAND_POLICY.md`.
- Versions through `v0.2.0-beta.1` retain their original MIT terms. Their tags,
  source archives, and release records remain unchanged.
- The public contributor record and predecessor history identify only
  `@recrack` or the SideRefresh maintainer as the source contributor. No
  external Swift package or separately licensed predecessor source is bundled.
  The maintainer authorized the cutover for project-owned work.

## Gate status

| Gate | Status before publication | Evidence |
| --- | --- | --- |
| `SRC-01` public source and repository controls | pass | Canonical Apache-2.0 license, `NOTICE`, brand policy, and public-source validators |
| `SRC-02` source-only prerelease preparation | pass | Versioned notes, source-build commands, and no binary asset |
| `SRC-03` requirements, privacy, scope, and support | pass | Release notes retain requirements and limitations without adding a device-compatibility claim |
| `SRC-04` immutable public tag and source archives | not-run | Verify the tag, generated archives, reviewed commit, prerelease status, and immutability immediately after publication |

No Developer ID binary, ad-hoc app bundle, provisioning profile, signing
credential, device identifier, or private project data is attached to this
prerelease. Every human-fixture and real-device acceptance row remains
`not-run` for this exact candidate. Stable `v0.2.0` remains blocked on Developer
ID signing, notarization, stapling, exact-archive Gatekeeper checks,
clean-account testing, and final real-device acceptance.
